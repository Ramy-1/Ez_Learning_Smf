
<form method="post" action="{{ path('app_universite_delete', {'iduni': universite.iduni}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ universite.iduni) }}">
    <button class="btn-dark">Supprimer</button>

</form>