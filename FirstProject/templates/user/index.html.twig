{% extends 'backbase.html.twig' %}

{% block title %}Hello CommandController!
{% endblock %}


{% block body %}
	<script>
		$("#zero_config").DataTable();
	</script>
	<div class="content">
		<div class="container-fluid">
			<div class="row">
				<div class="col-md-12">
					<div class="card strpied-tabled-with-hover">
						<div class="card-header ">
							<h4 class="card-title">Table Command</h4>
							<p class="card-category">les commands</p>
						</div>
						<div class="card-body table-full-width table-responsive">
							<table id="zero_config" class="table table-striped table-bordered">
								<thead>

									<th>email</th>
									<th>nom</th>
									<th>prenom</th>
									<th>role</th>
									{# <th>isVerified</th> #}
									<th></th>
									<th></th>
									{# <th></th> #}
								</thead>
								<tbody>
									{% for u in tab %}
										<tr>
											<td>{{u.email}}</td>
											{# <td>{{u.password}}</td> #}
											<td>{{u.name}}</td>
											<td>{{u.lastName}}</td>
											<td>
												{% for arole in u.roles %}
													<li>{{arole}}</li>
												{% endfor %}
											</td>
											{# <td>{{u.isVerified}}</td> #}
											<td>
												<a id="updated" href="{{ path('UserUpdate',{'id':u.id}) }}">Update</a>
												<i class="mdi mdi-account-convert"></td>
											</td>
											<td>
												<a href="{{ path('UserDelete',{'id':u.id}) }}">Delete</a>
												<i class="mdi mdi-account-multiple-minus"></td>
											</td>
											<td>
												<a href="{{ path('UserBlock',{'id':u.id}) }}">
													{% if u.isBlocked() %}
														Un
													{% endif %}
													Block
												</a>
												<i class="mdi mdi-account-alert"></td>
											</td>
										</tr>
									{% endfor %}
								</tbody>
							</div>
						</table>
					</div>
				</div>
			</div>
		</div>
	</div>


{% endblock %}
