{% extends 'base.html.twig' %}
{% block stylesheets %}
    {{parent()}}
{% endblock %}
{% block title %}Categories{% endblock %}
{% block javascripts %}
    {{parent()}}
{% endblock %}
{% block menu %}
    {{parent()}}
    <!-- Page wrapper  -->
      <!-- ============================================================== -->
      <div class="page-wrapper">
        <!-- ============================================================== -->
        <!-- Bread crumb and right sidebar toggle -->
        <!-- ============================================================== -->
        <div class="page-breadcrumb">
          <div class="row">
            <div class="col-12 d-flex no-block align-items-center">
              <h4 class="page-title">Categories</h4>
              <div class="ms-auto text-end">
                <nav aria-label="breadcrumb">
                  <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="#">Accuiel</a></li>
                    <li class="breadcrumb-item active" aria-current="page">
                      Categories
                    </li>
                  </ol>
                </nav>
              </div>
            </div>
          </div>
        </div>
{% endblock %}
{% block body %}

{{parent()}}

 <!-- ============================================================== -->
          <!-- Start Page Content -->
          <!-- ============================================================== -->
          <div class="row">
            <div class="col-md-12">
              <div class="card">
             
                  <div class="card-body">
    <h1 >Categorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idcat</th>
                <td>{{ categorie.idcat }}</td>
            </tr>
            <tr>
                <th>Domaine</th>
                <td>{{ categorie.domaine }}</td>
            </tr>
            <tr>
                <th>Nomcat</th>
                <td>{{ categorie.nomcat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categorie_index') }}" class="btn btn-info btn-sm text-white">Afficher la Liste</a>

    <a href="{{ path('app_categorie_edit', {'idcat': categorie.idcat}) }}" class="btn btn-warning btn-sm text-white">Modifier</a>

    {{ include('categorie/_delete_form.html.twig') }}
{% endblock %}
