{% extends 'base_front.html.twig' %}
{% block stylesheets %}
    {{parent()}}
    <style>
    
    </style>
{% endblock %}
{% block javascripts %}
    {{parent()}}
{% endblock %}
{% block body %}
{{parent()}}
<div class="intro-section single-cover" id="home-section">
      
      <div class="slide-1 " style="background-image: url('{{asset('images/img_2.jpg')}}');" data-stellar-background-ratio="0.5">
        <div class="container">
          <div class="row align-items-center">
            <div class="col-12">
              <div class="row justify-content-center align-items-center text-center">
                <div class="col-lg-6">
                  <h1 data-aos="fade-up" data-aos-delay="0"></h1>
                  <p data-aos="fade-up" data-aos-delay="100"></p>
                </div>

                
              </div>
            </div>
            
          </div>
        </div>
      </div>
    </div>
    <div class="site-section">
      <div class="container">
      <h1 >Cours</h1>
        <div class="row">
    <div class="col-md">

   {#} <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cour.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ cour.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ cour.description }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ cour.duree }}</td>
            </tr>
            <tr>
                <th>Support</th>
                <td>{{ cour.support }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ cour.etat }}</td>
            </tr>
            <tr>
                <th>Idcat</th>
                <td>{{ cour.idcat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cours_index') }}"  class="btn btn-success"><i class="bi bi-cart-plus"></i>back to list</a>

    <a href="{{ path('app_cours_edit', {'id': cour.id}) }}" class="btn btn-dark"><i class="bi bi-cart-plus"></i>edit</a>



    <a href="{{ path('coursarchiver',{'id':cour.id})}}" class="btn btn-success">
        <i class="bi bi-cart-plus"></i> archiver
        <a href="{{ path('coursinarchiver',{'id':cour.id})}}" class="btn btn-success">
            <i class="bi bi-cart-plus"></i> activer
</a>
    <a href="{{ path('app_categorie_show',{'idcat':cour.idcat})}}" class="btn btn-success">
        <i class="bi bi-cart-plus"></i> cat
    </a>


    {{ include('cours/_delete_form.html.twig') }}
#}
{% if app.user != null %}
<h3 >{{ cour.titre }}</h3>
  <div class="video" style="margin-left: 16%;" >
    <iframe width="727" height="409" class="responsive-iframe" src="https://www.youtube.com/embed/{{ cour.support }}" frameborder="0" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
  </div>
 <p>{{ cour.description }}</p>
        </div> 
         </div>
        <a href="{{ path('app_cours_index') }}"  class="btn btn-success"><i class="bi bi-cart-plus"></i>back to list</a>

    <a href="{{ path('app_cours_edit', {'id': cour.id}) }}" class="btn btn-dark"><i class="bi bi-cart-plus"></i>edit</a>



    <a href="{{ path('coursarchiver',{'id':cour.id})}}" class="btn btn-success">
        <i class="bi bi-cart-plus"></i> archiver
        <a href="{{ path('coursinarchiver',{'id':cour.id})}}" class="btn btn-success">
            <i class="bi bi-cart-plus"></i> activer
</a>
    <a href="{{ path('app_categorie_show',{'idcat':cour.idcat})}}" class="btn btn-success">
        <i class="bi bi-cart-plus"></i> cat
    </a>
 {% else %}
 <table class="table">
        <tbody>
            
            <tr>
                <th>Titre</th>
                <td>{{ cour.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ cour.description }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ cour.duree }}</td>
            </tr>
            
            
            
        </tbody>
    </table>
 {% endif %}
{% endblock %}
