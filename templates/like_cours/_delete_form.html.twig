
<form method="post" action="{{ path('app_like_cours_delete', {'idlike': like_cour.idlike}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ like_cour.idlike) }}">
    <button class="btn">Delete</button>
</form>
